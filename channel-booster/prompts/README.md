# Prompt library

Copy-paste prompts for any capable model (Claude, Grok, or another) when you are not running `booster ai`. Each prompt loads the same rules the engines use. Replace the `{{placeholders}}`. Paste the relevant playbook file after the prompt when the model has room for it.
