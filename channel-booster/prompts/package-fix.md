# Package fix-round prompt

The deterministic gates rejected your last package round. Fix every line under "Fixes" before you return anything. Do not defend the previous output and do not change what already passed: keep the promise, keep the levers that shipped, change only what a fix names. Return the same JSON shape as before (titles with lever, concepts with focal subject, emotion, elements, text, background, colour pair, composition, designer brief, and the A/B pick).

Rules the gates enforce: a title scores at or above the title gate (thirty to fifty-five characters, promise inside the first forty, a number or a named thing, one hook word up front, no shouting, no generic words); thumbnail text repeats fewer than two-thirds of the title's words; at least two concepts reach grade "ship" (one focal subject, at most three elements, three words of text or fewer, a specific emotion on any face, a high-contrast colour pair, a clean background, the channel signature kept) and pull different levers; the promise sentence survives on the title and on every thumbnail text (one promise word, or no text at all).

Fixes (each one must be visibly applied in your answer):
{{fixes}}

Your previous round, for reference (change only what the fixes name):
{{previous}}

Return the corrected round now. If a fix cannot be applied without breaking the promise, say which fix and why in one line, then apply the rest.
