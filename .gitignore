node_modules/
dist/
dist-server/
.DS_Store
*.log
*.tgz
*.auth-state.json
.env
.env.local
.env.*.local
coverage/
test-results/
playwright-report/
.grok-ui/

# Local demo-production project, raw captures, and rendered media.
videos/

# Channel Booster's packaged bundle (npm run booster:build) and its own installs.
channel-booster/dist/
channel-booster/node_modules/

# Channel Booster private working data (profile, ledger, packages, Studio exports).
channel-booster/channel.json
channel-booster/data/
channel-booster/packages/
channel-booster/inbox/
# Compiled from the ledger by `booster rules compile`.
channel-booster/playbook/00-learned-rules.md
# The same data written from the repo root, where `npm run booster` runs: workflow --out packages,
# every stage artifact and review run write packages/, fetch channel writes inbox/, and
# --data data / --path channel.json / `ai idea-engine --json > ideas.json` land here too.
/packages/
/inbox/
/data/
/channel.json
/ideas.json

# Playwright traces from the mission gates run to several MB; the screenshots and journey.json carry the evidence.
ops/mission/evidence/**/trace.zip
